# Playwright E2E Tests - Session Persistence Guide

## Обзор

Этот проект теперь включает комплексную систему e2e тестирования с Playwright, специально разработанную для тестирования сессионного управления и аутентификации в Keygen Customer Portal.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
cd keygen-customer-portal-frontend
pnpm install
```

### 2. Установка браузеров Playwright
```bash
pnpm run e2e:setup
```

### 3. Запуск тестов
```bash
# Запуск всех e2e тестов
pnpm run e2e

# Или отдельные наборы тестов
pnpm run e2e:auth        # Тесты аутентификации
pnpm run e2e:protected   # Тесты защищенных маршрутов
```

## 🔑 Ключевые особенности

### Сохранение сессии между тестами
- **Автоматическая аутентификация**: Глобальная настройка выполняет вход один раз
- **Состояние сессии**: Сохраняется в `e2e/.auth/user.json`
- **Переиспользование**: Все тесты используют сохраненную сессию

### Полный охват тестирования
- ✅ Вход и выход из системы
- ✅ Сохранение сессии при перезагрузке страницы
- ✅ Навигация между защищенными маршрутами
- ✅ Защита маршрутов от неавторизованного доступа
- ✅ Управление сессиями пользователя
- ✅ Смена пароля
- ✅ Выбор тем
- ✅ Мобильная адаптивность
- 🔄 **Полный путь пользователя**: Регистрация → Авторизация → Профиль → 3 перезагрузки → Выход

## 📋 Доступные команды

```bash
# Основные команды
pnpm run e2e                    # Запуск всех тестов
pnpm run e2e:ui                # Интерактивный режим с UI
pnpm run e2e:debug             # Режим отладки
pnpm run e2e:headed            # Видимый браузер

# По браузерам
pnpm run e2e:chromium          # Chromium
pnpm run e2e:firefox           # Firefox
pnpm run e2e:webkit            # WebKit (Safari)
pnpm run e2e:mobile            # Mobile Chrome

# Специфические тесты
pnpm run e2e:auth              # Только аутентификация
pnpm run e2e:protected         # Только защищенные маршруты

# Запуск конкретного теста
npx playwright test --grep "complete user journey"  # Полный путь пользователя

# Утилиты
pnpm run e2e:setup             # Установка браузеров
pnpm run e2e:report            # Просмотр отчетов
pnpm run e2e:codegen           # Генерация кода тестов
```

## 🔧 Настройка

### Переменные окружения

Создайте `.env` файл в корне frontend проекта:

```bash
TEST_USER_EMAIL=user@example.com
TEST_USER_PASSWORD=user123
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=admin123
TEST_GUEST_EMAIL=guest@example.com
TEST_GUEST_PASSWORD=guest123
```

### Структура файлов

```
keygen-customer-portal-frontend/
├── e2e/
│   ├── auth-fixtures.ts           # Фикстуры аутентификации
│   ├── global-setup.ts            # Глобальная настройка
│   ├── tests/
│   │   ├── auth-session.spec.ts      # Тесты сессий
│   │   └── protected-routes.spec.ts # Тесты защищенных маршрутов
│   ├── .auth/                     # Сохраненное состояние сессии
│   ├── .gitignore                 # Игнорирование чувствительных данных
│   └── README.md                  # Подробная документация
├── playwright.config.ts           # Конфигурация Playwright
└── package.json                   # Скрипты для запуска
```

## 🎯 Примеры использования

### Простой тест аутентификации

```typescript
import { test, expect, AuthHelper } from '../auth-fixtures';

test('should login and maintain session', async ({ page }) => {
  // Вход в систему
  await AuthHelper.login(page);

  // Проверка, что мы на dashboard
  await expect(page).toHaveURL(/\/dashboard/);

  // Перезагрузка страницы
  await page.reload();

  // Сессия должна сохраниться
  await expect(page).toHaveURL(/\/dashboard/);
});
```

### Тест защищенного маршрута

```typescript
test('dashboard functionality', async ({ authenticatedPage }) => {
  const page = authenticatedPage;

  // Страница уже аутентифицирована
  await page.goto('/dashboard');

  // Проверка элементов dashboard
  await expect(page.locator('[data-testid="dashboard"]')).toBeVisible();
});
```

## 🔍 Отладка и разработка

### Режимы отладки

1. **UI режим**: `pnpm run e2e:ui` - визуальный интерфейс Playwright
2. **Debug режим**: `pnpm run e2e:debug` - пошаговое выполнение
3. **Headed режим**: `pnpm run e2e:headed` - видимый браузер

### Создание новых тестов

```bash
# Генерация кода через взаимодействие
pnpm run e2e:codegen
```

### Просмотр результатов

- Отчеты сохраняются в `playwright-report/`
- Скриншоты при падении в `test-results/`
- Видео при падении (если включено)

## 🚀 CI/CD интеграция

Для использования в CI добавьте:

```yaml
# GitHub Actions пример
- name: Run E2E Tests
  run: pnpm run e2e:chromium
  env:
    CI: true
    TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
    TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
```

## 📊 Отчеты и метрики

- **Параллельное выполнение**: Тесты запускаются параллельно для скорости
- **Автоматические скриншоты**: При падении тестов
- **Трассировка**: Детальная трассировка действий
- **HTML отчеты**: Красивые отчеты с результатами

## 🔐 Безопасность

- Чувствительные данные не коммитятся (`.gitignore`)
- Переменные окружения для учетных данных
- Автоматическая очистка сессий
- Изоляция тестовых данных

## 📞 Поддержка

При проблемах:

1. Проверьте, что dev server запущен: `pnpm start`
2. Установите браузеры: `pnpm run e2e:setup`
3. Проверьте переменные окружения
4. Посмотрите отчеты: `pnpm run e2e:report`

---

## 🎉 Результат

Теперь у вас есть полноценная система e2e тестирования с:
- ✅ Сохранением сессии между тестами
- ✅ Автоматической аутентификацией
- ✅ Полным охватом функциональности
- ✅ Поддержкой нескольких браузеров
- ✅ Мобильным тестированием
- ✅ Богатыми отчетами и отладкой

Наслаждайтесь надежным тестированием! 🚀